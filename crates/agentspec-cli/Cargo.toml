[package]
name = "agentspec-cli"
version = "0.7.1"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "CLI scaffolding for tools — clap setup, shell completions, TTY detection"
keywords = ["cli", "clap", "shell-completions"]
categories = ["command-line-interface"]

[dependencies]
agentspec-update = { workspace = true }
clap = { workspace = true }
clap_complete = { workspace = true }
anyhow = { workspace = true }
